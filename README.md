# aws_static_pelican_website
Pelican document website using AWS s3 and possibly cloudfront


This repo uses python so I created venv environment in repo root.
Please make sure .gitignore has an entry for your venv

```
python -m venv mkdocs-venv
source mkdocs-venv/bin/activate

```